#random number feed generater 
import sys
print ('Systems avalible: sysmain, sysone, systwo, sysshrimp')
print ('Warning! Make sure all entered words are all lower case')
system = input ('Enter system name:')
number = input ('Enter number of feeds:')
number_list = range.split(1,)
	if system == sysmain:


	elif system == sysone:

	elif system == systwo:

	elif system == syshrimp:

	else:
			print('Have a good day and try again')
			sys.exit(0)
